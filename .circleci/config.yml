version: 2.1

jobs:
  test:
    docker:
      - image: circleci/node:latest # Użyj obrazu Docker z Node.js
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: chmod +x ./node_modules/.bin/jest && npm test
